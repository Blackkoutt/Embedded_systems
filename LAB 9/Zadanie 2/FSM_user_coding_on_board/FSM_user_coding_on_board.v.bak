module FSM_user_coding (input [0:0] KEY,
								input [1:0] SW,
								output [9:0] LEDR);
								
	FSM_user_coding FSM(.clk(~KEY[0]), .aclr(SW[0]), .w(SW[1]),
								.z(LEDR[9]), .y(LEDR[8:0]));
	
endmodule
